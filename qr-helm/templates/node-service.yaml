apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.servicenode.name }}
spec:
  type: ClusterIP
  selector:
    app: node
  ports:
    - protocol: TCP
      port: {{ .Values.servicenode.port }}
      targetPort: {{ .Values.nodePort }}
      
